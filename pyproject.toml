[project]
name = "caffeine"
version = "0.1.0"
description = "Attention optimizer benchmark harness"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.19.0",
    "numpy>=2.0.0",
    "torch>=2.7.0",
    "transformers>=5.11.0",
]

[tool.uv]
package = false
